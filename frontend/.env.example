# Copy this file to .env.local (Vite loads .env.local automatically and it's
# gitignored by default) and adjust if your backend isn't on localhost:8000.

# Base URL of the VarshaNetra FastAPI inference service
# (PYTHONPATH=src uvicorn varshanetra.inference.api:app --reload)
VITE_API_BASE_URL=http://localhost:8000
